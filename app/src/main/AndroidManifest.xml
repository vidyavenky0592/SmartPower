<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.dominionenergy.vidyavenkatesh.dominionenergy">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".LoginActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".CreateAccount">
        </activity>
        <activity android:name=".WriteReport">
        </activity>
        <activity android:name=".ReportIncident">
        </activity>
        <activity android:name=".NoCustomerMenu">
        </activity>
        <activity android:name=".SmartHome">
        </activity>
        <activity android:name=".SignInMenu">
        </activity>
        <activity android:name=".popUp">
        </activity>
        <activity android:name=".Payment">
        </activity>
        <activity android:name=".EarnPoints">
        </activity>
        <activity android:name=".Recharge_menu">
        </activity>
        <activity android:name=".TipsEnergy">
        </activity>
        <activity android:name=".Alert">
        </activity>
        <activity android:name=".Safety">
        </activity>
    </application>

</manifest>